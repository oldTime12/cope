import React from "react";
import Hero from "@/app/components/hero";

export default function Scale() {
  return (
    <Hero
      imgUrl={"/scale.png"}
      altTxt="scale pic"
      content="scale's ploblem is important."
    />
  );
}
